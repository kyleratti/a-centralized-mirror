PORT=3010
# the port to listen on

API_TOKEN=""
# the API token that requests should be authenticated with
API_ADMIN_TOKEN=""
# the API token that requests for administrative tasks should be authenticated with
API_ADMIN_IP=""
# the IP address, if any, allowed to authenticate against the admin API
API_CRONTAB_TOKEN=""
# the API token that requests for the crontab service should be authenticated with
API_CRONTAB_IP=""
# the IP address, if any, allowed to authenticate against the crontab API

DATABASE_LOCATION="./db/database.sqlite"
# the full path to the sqlite database file
